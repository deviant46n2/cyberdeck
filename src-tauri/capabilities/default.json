{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the main window: core plugin permissions including core:event so the frontend can register the store/console event listeners. Without this file Tauri v2's ACL denies the event plugin and every frontend `listen()` silently fails while invoke commands keep working.",
  "windows": ["main"],
  "permissions": ["core:default"]
}